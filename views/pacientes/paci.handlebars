{{> navbar}}
<div class="container page-margin-top mb-5">
  <div class="row">
    <div class="col-2">{{> navPacientes}}</div>
    <div class="col-10">
      <h1>Pacientes</h1>

      <div class="row">
        {{#each listar}}
        <div class="col">
          <strong> #{{this.id}} </strong>
          <a href="/pacientes/paci/{{this.id}}">{{this.nome}}</a><br>
        </div>
        {{/each}}

      </div>
    </div>
  </div>
</div>